val list = [2, 3, 4];
(* hd -> head *)
val first = hd list;
(* tl -> tail *)
val two_last = tl list;
val isEmpty = null list;
val last_one = tl two_last

val another_list = [1, 3, 4, 5, 5, 6, 7, 2];
val last_ = tl(tl(tl another_list));
